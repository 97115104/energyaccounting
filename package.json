{
  "name": "eaj",
  "private": true,
  "scripts": {
    "dev": "bun run db:migrate && bun run --filter @eaj/server dev",
    "dev:web": "bun run --filter @eaj/web dev",
    "build": "bun run --filter @eaj/shared build && bun run --filter @eaj/web build",
    "start": "bun run --filter @eaj/server start",
    "db:backup": "bun apps/server/src/db/backup.ts",
    "db:migrate": "bun apps/server/src/db/migrate.ts",
    "db:verify": "bun apps/server/src/db/verify.ts",
    "typecheck": "bun run --filter @eaj/shared typecheck && bun run --filter @eaj/server typecheck && bun run --filter @eaj/web typecheck",
    "test": "bun test packages/shared apps/server apps/web/src/lib",
    "icons": "bun scripts/generate-icons.ts",
    "readme-marks": "bun scripts/generate-readme-marks.ts",
    "generate-more-invite-codes": "bun scripts/generate-more-invite-codes.ts"
  },
  "workspaces": ["apps/*", "packages/*"]
}
